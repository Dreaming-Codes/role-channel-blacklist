# Discord Bot Configuration
DISCORD_TOKEN=your-discord-bot-token-here

# Optional: Bot prefix for text commands (default is no prefix for slash commands)
# BOT_PREFIX=!

# Optional: Database path (default is ./data)
# DB_PATH=./data

# Optional: Log level (debug, info, warn, error)
# RUST_LOG=info
